<%= form_for(@h) do |f| %>
  <% if @h.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@h.errors.count, "error") %> prohibited this h from being saved:</h2>

      <ul>
      <% @h.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
